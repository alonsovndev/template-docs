# Template Docs

<!-- TODO: replace your-org with your GitHub org/user once the repo is published -->

[![Deploy to GitHub Pages](https://github.com/your-org/template-docs/actions/workflows/deploy.yml/badge.svg)](https://github.com/your-org/template-docs/actions/workflows/deploy.yml)

A documentation site template built with [Docusaurus](https://docusaurus.io/). Use it as the starting point for your project's documentation: it ships with a minimal, working structure, autogenerated sidebars, and a GitHub Pages deployment workflow.

## 📑 Table of Contents

- [Template Docs](#template-docs)
  - [📑 Table of Contents](#-table-of-contents)
  - [✨ Features](#-features)
  - [📂 Documentation Structure](#-documentation-structure)
  - [🤖 Generating Docs with AI Agents](#-generating-docs-with-ai-agents)
  - [🚀 Getting Started](#-getting-started)
    - [⚙️ Prerequisites](#️-prerequisites)
    - [💾 Installation](#-installation)
    - [🏃 Running Locally](#-running-locally)
  - [🛠️ Template Setup Checklist](#️-template-setup-checklist)
  - [🔗 Important Links](#-important-links)
  - [🤝 Contributing](#-contributing)

## ✨ Features

- TypeScript-based Docusaurus (classic preset) configuration
- Docs-only layout
- Autogenerated sidebar from the `docs` folder structure
- Dark mode that follows the system preference
- GitHub Actions workflow for GitHub Pages deployment
- Pre-built documentation scaffolds: context, requirements, planning, architecture, decisions, and prototype

## 📂 Documentation Structure

```
docs/
├── intro.md                 # Template guide and adoption steps
├── 00-context/              # Project overview, personas, glossary, open questions, out of scope
├── 01-requirements/         # Feature map + per-feature requirement template
├── 02-planning/             # Phased roadmap and role mapping
├── 03-architecture/         # Core, API, database, diagrams, ops, security
├── 04-decisions/            # ADR decision log + ADR template
└── 05-prototype/            # Prototype brief, design direction, Pencil files
```

Work items (epics, user stories) live in **Jira** for production projects. The
`examples/work-items/` folder at the repo root contains a filled-in reference
instance — safe to delete when starting a new project.

## 🤖 Generating Docs with AI Agents

This repo ships the same set of guided `/docs-*` slash commands for three AI coding
agents — pick whichever you use:

| Agent                                                      | Commands live in                   |
| ---------------------------------------------------------- | ---------------------------------- |
| [Claude Code](https://docs.claude.com/en/docs/claude-code) | `.claude/commands/`                |
| [opencode](https://opencode.ai/)                           | `.opencode/commands/`              |
| [GitHub Copilot](https://docs.github.com/en/copilot)       | `.github/prompts/` (`*.prompt.md`) |

Each command interviews you (up to 10 questions per section) and writes your answers
directly into `docs/`, instead of manual search-and-replace.

| Command              | Purpose                                                                                                                               |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `/docs-setup`        | One-time template setup — replaces `[Project Name]`, updates `docusaurus.config.ts` and `README.md`                                   |
| `/docs-next`         | Read-only status check — reports completion per section and tells you which command to run next                                       |
| `/docs-context`      | Fills `00-context` (overview, personas, glossary, out-of-scope)                                                                       |
| `/docs-requirements` | Creates one feature doc per feature in `01-requirements`                                                                              |
| `/docs-planning`     | Fills `02-planning` (phased roadmap, role mapping)                                                                                    |
| `/docs-decisions`    | Creates one ADR per decision in `04-decisions`                                                                                        |
| `/docs-architecture` | Fills `03-architecture` one subfolder per run (core → database → api → security → ops → diagrams), drafting ADRs as decisions come up |
| `/docs-prototype`    | Fills `05-prototype` (prototype brief, design direction)                                                                              |
| `/docs-work-items`   | Generates Jira-ready epics/stories into `work-items/` from the completed docs                                                         |

Run `/docs-setup` first, then run the rest in order, since each section depends on the
ones before it:
`/docs-context` → `/docs-requirements` → `/docs-planning` → `/docs-decisions` →
`/docs-architecture` → `/docs-prototype` → `/docs-work-items`. Run `/docs-next` at any
point to check progress and get the next recommended command.

`/docs-architecture` runs one subfolder per invocation — re-run it until all six are
done. It also writes ADRs into `04-decisions` as decisions come up, so `/docs-decisions`
is for decisions that don't belong to an architecture pass.

None of these agents set up? Fill in the placeholders manually following the same order —
see [`docs/intro.md`](./docs/intro.md).

## 🚀 Getting Started

### ⚙️ Prerequisites

- Node.js >= 20
- npm >= 10
- Git

### 💾 Installation

```sh
# Clone the repository
git clone https://github.com/your-org/template-docs.git
cd template-docs

# Install dependencies
npm install
```

### 🏃 Running Locally

```sh
# Start the dev server with hot reload
npm start
```

This opens the site at **http://localhost:3000**. The server watches for file changes and reloads automatically.

| Command             | Description                        |
| ------------------- | ---------------------------------- |
| `npm start`         | Start the dev server (hot reload)  |
| `npm run build`     | Build for production into `build/` |
| `npm run serve`     | Serve the production build locally |
| `npm run typecheck` | Run the TypeScript compiler check  |
| `npm run clear`     | Clear the Docusaurus cache         |

## 🛠️ Template Setup Checklist

Before deploying your own site based on this template, replace the placeholder values:

1. In `docusaurus.config.ts`:
   - `title`, `tagline` — your project name and description
   - `url` — production URL, e.g. `https://<organizationName>.github.io`
   - `baseUrl` — use `/` for custom domains or user/org sites, `/<projectName>/` for GitHub Pages project sites
   - `organizationName`, `projectName` — your GitHub org/user and repository name
   - `editUrl` — point to your repository to enable "Edit this page" links
   - Navbar and footer links — your GitHub repository and community links
2. In this `README.md` — replace the badge URL and repository links
3. Fill in every `[Project Name]` and `YYYY-MM-DD` placeholder in `docs/` using search-and-replace
4. Replace `static/img/logo.svg` and `static/img/favicon.ico` with your branding
5. Delete `examples/` if you don't need the reference work items
6. For GitHub Pages: in the repository settings, set **Settings → Pages → Source** to **GitHub Actions**

## 🔗 Important Links

- [Docusaurus Documentation](https://docusaurus.io/docs)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Markdown Features](https://docusaurus.io/docs/markdown-features)

## 🤝 Contributing

1. Create a feature branch from `main`
2. Write your documentation or template changes
3. Verify locally with `npm run build` (broken links fail the build)
4. Open a pull request against `main`; merging deploys the site via GitHub Actions
